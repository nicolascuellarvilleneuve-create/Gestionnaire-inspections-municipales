# Guide d'Installation (Nouveau PC)

Voici comment installer et lancer l'application sur un autre ordinateur.

## Pré-requis
1.  **Node.js**: Assurez-vous que Node.js est installé (téléchargez la version "LTS" sur [nodejs.org](https://nodejs.org/)).
2.  **Git**: Assurez-vous que Git est installé (pour cloner le projet).

## Installation

1.  **Ouvrez un terminal** (PowerShell ou CMD).
2.  **Clonez le projet** (téléchargez le code) :
    ```bash
    git clone https://github.com/nicolascuellarvilleneuve-create/Gestionnaire-inspections-municipales.git
    ```
3.  **Entrez dans le dossier** :
    ```bash
    cd Gestionnaire-inspections-municipales
    ```
4.  **Installez les dépendances** :
    ```bash
    npm install
    ```

## Lancement

Une fois installé, double-cliquez simplement sur le fichier :
👉 **`LANCER_APP.bat`**

Cela démarrera le serveur et ouvrira l'application dans votre navigateur.
